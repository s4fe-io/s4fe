# build:
#   docker:
#     web: Dockerfile
#   config:
#     DISABLE_COLLECTSTATIC: 1
# run:
#   web: waitress-serve --port=8000 s4fe.wsgi:application
# release:
#   image: web
#   command:
#     - python3 manage.py migrate
